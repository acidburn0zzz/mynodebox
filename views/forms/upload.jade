#upload
  form(action="/upload", method="post", enctype="multipart/form-data")
    label= locale.fileslist.sendfile
      input(type="file", name="uploadfile")
      input(type="submit", name="sendfile", value=locale.fileslist.sendfilesubmit)